- **동기**: 기존 라운드 안내 UI가 실제 서버 통신 속성인 `turn`과 불일치하거나, 텍스트가 달라 오해가 생길 여지가 있음.
- **의사 결정**:
  - `game:patch`를 통해 받은 `turn` 속성을 보다 명확하게 클라이언트 상태의 기준으로 삼기로 함.
  - 관련 UI 컴포넌트(`GamePage`, `GameBoard`)의 표출 문맥을 교정합니다.
